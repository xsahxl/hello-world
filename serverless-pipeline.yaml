name: "Hello world Serverless-cd"

env:
  name: hello world
  age: 18
steps:
  - run: echo 'Hi ${{ env.name }}'
  - run: echo 'Hi ${{ env.name }}'
    env:
      name: Tony
  - run: echo 'Hi ${{ task.id }}'
    id: task
